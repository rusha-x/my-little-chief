<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/arrowView"
        app:srcCompat="@drawable/recipe_arrow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/recipesItem"
        app:layout_constraintBottom_toBottomOf="@id/recipesItem"
        android:layout_marginEnd="16dp"
        />

    <TextView
        android:id="@+id/recipesItem"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        tools:text="RecipeName"
        android:textSize="30sp"
        android:layout_marginStart="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/arrowView"
        app:layout_constraintTop_toTopOf="parent"
        android:maxLines="3"
        android:ellipsize="end"
        android:layout_marginVertical="16dp"
        />

</androidx.constraintlayout.widget.ConstraintLayout>